function somatorioIndices(array, index1, index2) {
    if (!Array.isArray(array) || array.length === 0) {
      return 0;
    }
  
    let soma = 0;

    for (let i = a; i <= b; i++) {
      const index = i % array.length; // índice real no array repetido
      sum += array[index];
    }
  
    return sum;
  }