/* 
let contador = 0

function iterarMatrizes(matriz){
    const novaMatriz = [...matriz]
    for(let linha = 0; linha < 3; linha++){
        
        for (let coluna = 0; coluna < 3; coluna++){
            novaMatriz[linha][coluna] = 0
            
        }
    }
    
    setTimeout(() => console.table(novaMatriz), 100000)
}


const aMinhaMatriz = [
        ['1','2','2'],
        ['1','2','2'],
        ['1','2','2'],
        ]

        
 iterarMatrizes(aMinhaMatriz)

 */

/* function iterarMatrizes(){
    let pyramidToReturn = ""
    for(let linha = 0; linha < 3; linha++){
        
        pyramidToReturn += "."
        for (let coluna = 0; coluna < 3; coluna++){
            
           
        }
    }
    
    setTimeout(() => console.table(novaMatriz), 100000)
}
iterarMatrizes() */